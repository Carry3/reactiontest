# ===== 应用配置 =====
spring.application.name=cognitive-reaction-test
server.port=8080

# ===== PostgreSQL 数据库配置 =====
spring.datasource.url=jdbc:postgresql://14.103.129.142:5440/cognitive_test_db
spring.datasource.username=group4
spring.datasource.password=5100Group4
spring.datasource.driver-class-name=org.postgresql.Driver

# ===== JPA 配置 =====
spring.jpa.hibernate.ddl-auto=update
# 选项说明：
# - create: 每次启动删除并重建表（开发初期可用）
# - update: 自动更新表结构（推荐开发时使用）
# - validate: 只验证不修改
# - none: 不做任何操作（生产环境推荐）

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===== JWT 配置 =====
# 密钥（生产环境应使用更复杂的密钥，至少32个字符）
jwt.secret=BLlZM0mnc5WjoPp6rXzEhsfB5xtoUATgqSywkPJ11RE5NdXSve
# Token过期时间（毫秒）：24小时 = 86400000ms
jwt.expiration=86400000

# ===== 日志配置 =====
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===== 邮件配置 =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chen121666@gmail.com
spring.mail.password=xtfhkebsipivagsr

# Gmail SMTP 配置 (使用 STARTTLS)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ===== 前端配置 =====
app.frontend-url=http://localhost:3000

# ===== CORS 跨域配置 =====
# 允许的前端来源（多个用逗号分隔）
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# ===== 启动通知配置 =====
# 是否启用启动邮件通知
app.startup-notification.enabled=false
# 启动通知邮件接收者
app.startup-notification.recipient=chen121666@gmail.com

# ===== 异步任务配置 (用于发送邮件) =====
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5
spring.task.execution.pool.queue-capacity=100